# 学生管理系统 — 运行说明

## 一、环境要求

在运行项目之前，请确保已安装以下软件：

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | >= 18.0.0 | JavaScript 运行环境 |
| npm | >= 9.0.0 | Node.js 包管理器 |
| MySQL | >= 8.0 | 数据库 |

### 检查环境

```bash
# 检查 Node.js 版本
node -v

# 检查 npm 版本
npm -v

# 检查 MySQL 版本
mysql --version
```

---

## 二、首次运行（初始化）

### 2.1 安装依赖

```bash
# 安装后端依赖
cd backend
npm install

# 安装前端依赖
cd ../frontend
npm install
```

### 2.2 配置环境变量

**后端配置** - 复制 `.env.example` 为 `.env` 并修改：

```bash
cd backend
cp .env.example .env
```

编辑 `.env` 文件：
```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=student_management

# JWT配置
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=7d

# 服务器配置
PORT=3000
```

**前端配置** - 复制 `.env.example` 为 `.env`：

```bash
cd frontend
cp .env.example .env
```

### 2.3 初始化数据库

```bash
cd backend
npm run init-db
```

这将创建数据库表并插入测试数据。

---

## 三、启动项目

### 3.1 启动后端服务器

打开一个终端窗口：

```bash
cd backend
npm run dev
```

**输出示例：**
```
╔════════════════════════════════════════════════════════════╗
║                                                            ║
║   🎓 学生管理系统后端服务已启动                              ║
║                                                            ║
║   📍 地址: http://localhost:3000                          ║
║   📍 API:  http://localhost:3000/api/v1                   ║
║   📍 健康检查: http://localhost:3000/health               ║
║                                                            ║
╚════════════════════════════════════════════════════════════╝
```

### 3.2 启动前端服务器

**新开一个终端窗口**：

```bash
cd frontend
npm run dev
```

**输出示例：**
```
  VITE v5.4.21  ready in 365 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 3.3 访问系统

打开浏览器访问：**http://localhost:5173/**

使用测试账号登录：
- 管理员：`admin` / `123456`
- 教师：`T202401` / `123456`
- 辅导员：`C202401` / `123456`
- 学生：`23010001` / `123456`

---

## 四、关闭项目

### 4.1 方法一：Ctrl + C（推荐）

在每个运行中的终端窗口按 `Ctrl + C` 停止服务：

- 在前端终端按 `Ctrl + C` → 关闭前端服务
- 在后端终端按 `Ctrl + C` → 关闭后端服务

### 4.2 方法二：强制关闭所有 Node 进程

**Windows：**
```bash
taskkill /F /IM node.exe
```

**Mac/Linux：**
```bash
pkill -f node
```

> ⚠️ 注意：这会关闭所有 Node.js 进程，包括其他项目的进程。

---

## 五、常用命令速查表

### 5.1 后端命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式启动（自动重启） |
| `npm start` | 生产模式启动 |
| `npm run init-db` | 初始化数据库 |
| `npm test` | 运行测试 |
| `npm run lint` | 代码检查 |
| `npm run lint:fix` | 自动修复代码问题 |

### 5.2 前端命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式启动 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产版本 |
| `npm test` | 运行测试 |

### 5.3 快速启动

```bash
# 一键启动后端（在 backend 目录）
cd backend && npm run dev

# 一键启动前端（在 frontend 目录，新终端）
cd frontend && npm run dev
```

---

## 六、常见问题

### 6.1 端口被占用

**错误信息：** `Error: listen EADDRINUSE: address already in use :::3000`

**解决方法：**
```bash
# Windows - 查找占用端口的进程
netstat -ano | findstr :3000

# 杀死进程（替换 PID 为实际进程ID）
taskkill /F /PID <PID>

# 或者直接杀死所有 Node 进程
taskkill /F /IM node.exe
```

### 6.2 数据库连接失败

**错误信息：** `Error: Access denied for user 'root'@'localhost'`

**解决方法：**
1. 检查 `.env` 文件中的数据库配置
2. 确保 MySQL 服务已启动
3. 确认用户名和密码正确

### 6.3 依赖安装失败

**解决方法：**
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules
npm install
```

### 6.4 前端页面空白

**解决方法：**
1. 检查后端服务是否正常运行
2. 打开浏览器开发者工具（F12）查看控制台错误
3. 确认 API 地址配置正确

---

## 七、开发模式 vs 生产模式

| 特性 | 开发模式 (`npm run dev`) | 生产模式 (`npm start`) |
|------|--------------------------|------------------------|
| 自动重启 | ✅ 代码修改后自动重启 | ❌ 需要手动重启 |
| 热更新 | ✅ 支持 | ❌ 不支持 |
| 调试信息 | ✅ 详细日志 | ❌ 精简日志 |
| 性能 | 较低 | 较高 |
| 适用场景 | 开发调试 | 生产部署 |

---

## 八、项目结构

```
student-management-system/
├── backend/                 # 后端项目
│   ├── src/                # 源代码
│   ├── database/           # 数据库脚本
│   ├── scripts/            # 工具脚本
│   ├── __tests__/          # 测试文件
│   ├── package.json        # 依赖配置
│   └── .env                # 环境变量（需创建）
│
├── frontend/               # 前端项目
│   ├── src/               # 源代码
│   ├── public/            # 静态资源
│   ├── package.json       # 依赖配置
│   └── .env               # 环境变量（需创建）
│
└── docs/                   # 项目文档
    ├── 学生管理系统_运行说明.md
    ├── 学生管理系统_接口说明.md
    ├── 学生管理系统_数据库设计.md
    └── ...
```

---

## 九、联系方式

如有问题，请查阅项目文档或提交 Issue。